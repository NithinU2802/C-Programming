/**************************************************

    program to implement the digit count using Access 
Specifier.

**************************************************/

#include <stdio.h>

int main() {
    int n,c;
    scanf("%d%n",&n,&c);
    printf("Count: %d",c);
    return 0;
}
